[project]
name = "gpu-monitor"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "fastapi>=0.122.0",
    "pynvml>=13.0.1",
    "uvicorn>=0.38.0",
]

[dependency-groups]
dev = [
    "isort>=7.0.0",
    "ruff>=0.14.6",
]
